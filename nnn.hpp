#ifndef nnn_HPP
# define nnn_HPP

# include <iostream>
# include <string>

class nnn
{

	public:
	// Attributes

	// Constructor & Destructor
		nnn( void );
		nnn( nnn const &obj );
		nnn &operator = ( nnn const &obj );
		virtual ~nnn( void );

	// Methods

	private:
	// Attributes

	// Methods

};

#endif
